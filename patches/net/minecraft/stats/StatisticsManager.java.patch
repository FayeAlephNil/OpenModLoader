--- /net/minecraft/stats
+++ /net/minecraft/stats
@@ -47,7 +47,16 @@
         }
     }
 
-    public void unlockAchievement(EntityPlayer var1, StatBase var2, int var3) {
+    public void unlockAchievement(EntityPlayer player, StatBase statBase, int data) {
+        StatBase var2 = statBase;
+        int var3 = data;
+        if (statBase.isAchievement()) {
+            xyz.openmodloader.event.impl.player.EventAchievement event = xyz.openmodloader.event.impl.player.EventAchievement.handle(player, (Achievement) statBase, data);
+            var2 = event.achievement;
+            var3 = event.data;
+            if (event.isCanceled()) return;
+        }
+
         TupleIntJsonSerializable var4 = (TupleIntJsonSerializable)this.statsData.get(var2);
         if(var4 == null) {
             var4 = new TupleIntJsonSerializable();
